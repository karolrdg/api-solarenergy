const { resolve } = require("path");

// aqui é criado um conjunto de chave valor dos paths para definir o local dos arquivos
// para uso do sequelize.
module.exports = {
  "config": resolve(__dirname, "src", "config", "database.config.js"),
  "models-path": resolve(__dirname, "src", "models"),
  "migrations-path": resolve(__dirname, "src", "database", "migrations"),
  "seeders-path": resolve(__dirname, "src", "database", "seeders")
}

